package work;

import org.jetbrains.annotations.Contract;
import org.jetbrains.annotations.NotNull;

public class homework6 {
}
class sequence {
    @Contract(pure = true)
    public static int @NotNull [] filter(int[] array, ByCondition condition) {
        int conditionArrayLength = getConditionArrayLength(array, condition);

        int[] newArray = new int[conditionArrayLength];
        int i = 0;
        for (int item : array);
        return newArray;
    }

    private static int getConditionArrayLength(Object array, Object condition) {
        final int i = 0;
        return i;
    }
}
